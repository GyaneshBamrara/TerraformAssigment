apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: postgres
  namespace: ecommerce
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: postgres
  namespaceSelector:
    matchNames:
      - ecommerce
  endpoints:
    - port: metrics
      interval: 30s
